<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.StudentMapper">

    <select id="getAllStudents" resultType="com.example.demo.model.Student">
        SELECT *
        FROM Student
    </select>

    <select id="findStudentByName" resultType="com.example.demo.model.Student">
        SELECT *
        FROM Student
        WHERE studentName = #{studentName}
    </select>


    <select id="getStudentById" parameterType="int" resultType="com.example.demo.model.Student">
        SELECT *
        FROM Student
        WHERE studentId = #{studentId}
    </select>

    <insert id="createStudent" parameterType="com.example.demo.model.Student" useGeneratedKeys="true" keyProperty="studentId">
        INSERT INTO Student (studentName, studentAddress)
        VALUES (#{studentName}, #{studentAddress})
    </insert>

    <update id="updateStudentById" parameterType="map">
        UPDATE Student
        SET studentName=#{student.studentName},
        studentAddress=#{student.studentAddress}
        WHERE studentId=#{studentId}
    </update>


    <delete id="deleteStudentById" parameterType="int">
        DELETE
        FROM Student
        WHERE studentId=#{studentId}
    </delete>

</mapper>
